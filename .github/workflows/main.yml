name: "first workflow"

on: push

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "HelloW"
  job2:
    runs-on: ubuntu-latest
    steps:
      - name: Afficher WD
        run: pwd
      - uses: actions/checkout@v4
      - run: echo "second step"
      - run: ls -a
      
      
    
